package main

import (
	"fmt"
  "os"
	"os/exec"
	"strings"
)

func main() {
	cmd := exec.Command("bash", "-c", "ps aux | grep 'waybar' | grep -v grep | grep -v waybar-toggle")
	cmd.Stdout = os.Stdout
	err := cmd.Run()
  if err != nil {
    fmt.Println(err)
  }
}
